import { Direction } from '@/models/Grid'
import { doesNotCollide } from '@/models/Grid/utils'
import Square from '@/models/Shape/Square'

describe('Grid utils', () => {
  describe('collision detection with other shapes', () => {
    it('detects a DOWN collision', () => {
      const currentShape = new Square()
      const direction = Direction.DOWN
      const grid = 

    })
  })
})
  